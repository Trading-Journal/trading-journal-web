import { Portal } from '../../components/portal/Portal';
import { Register } from '../../containers/user/Register';

export const SignUp = () => {
  return (
    <Portal>
      <Register />
    </Portal>
  );
};
