import { Login } from '../../components/login/Login';
import { Portal } from '../../components/portal/Portal';

export const SignIn = () => {
  return (
    <Portal>
      <Login />
    </Portal>
  );
};
