import { Portal } from '../../components/portal/Portal';
import { Login } from '../../components/user/Login';

export const SignIn = () => {
  return (
    <Portal>
      <Login />
    </Portal>
  );
};
