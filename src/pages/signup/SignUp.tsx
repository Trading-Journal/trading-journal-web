import { Portal } from '../../components/portal/Portal';
import { Register } from '../../components/user/Register';

export const SignUp = () => {
  return (
    <Portal>
      <Register />
    </Portal>
  );
};
