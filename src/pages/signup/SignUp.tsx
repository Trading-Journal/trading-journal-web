import { Portal } from '../../components/portal/Portal';
import { Register } from '../../components/register/Register';

export const SignUp = () => {
  return (
    <Portal>
      <Register />
    </Portal>
  );
};
